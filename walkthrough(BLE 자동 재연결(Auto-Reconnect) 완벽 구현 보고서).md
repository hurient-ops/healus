# BLE 자동 재연결(Auto-Reconnect) 완벽 구현 보고서

이전 단계에서 계획하고 승인해주신 **'블루투스 자동 복구 체계'**를 성공적으로 모바일 프로젝트(`healus`)에 모두 적용했습니다. 이제 Healus 앱은 좀비처럼 끈질기게 펌프와의 통신을 유지합니다!

## 🛠️ 주요 변경 사항
### 1. 기기 고유 식별자(ID) 영구 기억
* `shared_preferences` 플러그인을 도입하여, 최초 연결 성공 시 블루투스 기기 식별자를 앱에 각인시켰습니다.
* **크로스플랫폼 대응**: iOS의 UUID 방식과 안드로이드의 MAC 주소 방식을 라이브러리가 공통(`remoteId.str`)으로 처리하도록 추상화하여, 양쪽 모바일 OS 환경 모두에서 문제없이 기기를 기억합니다.

### 2. 15초 단위의 백그라운드 재연결 루프 (좀비 프로세스)
* 사용자가 직접 '연결 해제'를 누르지 않았는데 연결이 끊어지는 상황(예: 거리 이탈, 방전, 간섭)이 감지되면, **즉시 15초 단위의 무한 재연결 루프(Timer)**가 가동됩니다.
* 이 루프는 앱이 켜져 있는 동안 계속 돌면서 기기를 찾고, 연결에 성공하면 스스로 루프를 멈춥니다.

### 3. 안전장치: 패킷 큐(Queue) 폭파
* 비정상적으로 연결이 단절되는 그 즉시, 기존에 대기하고 있던 `inject_controller` 내의 인슐린 주입/제어 명령 큐를 완전히 비워버리도록(Clear) 코딩했습니다.
* **의료 사고 완벽 차단**: 나중에 펌프와 다시 연결되었을 때 과거에 눌렀던 주입 버튼 명령이 오발송되는 것을 근본적으로 막았습니다.

### 4. 뒤로가기 2번 종료 시 대응 (Startup Recovery)
* 앱이 구동되는 시점(`main.dart`의 `initState`)에 즉시 영구 저장소를 뒤져보고, 저장된 펌프 ID가 있다면 다이얼로그 스캔 과정 없이 즉시 백그라운드 연결을 시도하도록 구현했습니다.

## ✅ 다음 추천 작업 (사용자 검증)
> [!TIP]
> 앱을 재빌드(APK 추출 중)하여 테스트 폰에 설치하신 뒤, 아래 두 가지 테스트를 꼭 진행해 보시길 권장해 드립니다.

1. **거리 이탈 테스트**: 기기가 연결된 상태에서 블루투스를 끄거나 멀리 도망갔다가 다시 돌아와 봅니다. 앱이 스캔 버튼을 누르지 않아도 스스로 기기를 찾아서 복구하는지 확인합니다.
2. **명시적 해제 테스트**: 설정 화면에서 "장치 연결 해제"를 누른 후, 앱을 껐다 켰을 때 **절대로** 펌프와 자동 연결되지 않는지(초기화 검증) 확인합니다.
