# BLE 통신 안정성 개선 보고서 (버그 픽스 완료)

제보해주신 3가지 현상에 대해 코드를 심층 분석하고 구조적인 문제점들을 모두 걷어내어 완벽하게 수정을 완료했습니다. 현재 적용된 해결 방안은 다음과 같습니다.

## 🛠️ 적용된 해결 사항

### 1. 거리 이탈 시 앱 화면 먹통(주입중 멈춤) 현상 완벽 해결
* **원인**: 통신 단절 시 패킷 큐는 초기화했지만, UI의 '주입중' 락(Lock)을 풀어주는 코드가 빠져있었습니다.
* **해결**: 연결이 비정상적으로 끊어짐을 감지하는 즉시 큐를 비움과 동시에 `resetInjectingState()` 함수가 호출되도록 수정했습니다. 이제 멀리 떨어져서 통신이 끊어지면, 화면의 주입 중 표시가 즉각 해제되고 **'대기 화면'**으로 돌아갑니다.

### 2. '뒤로가기 2번 종료' 후 앱 재실행 불가 현상 완벽 해결
* **원인**: `SystemNavigator.pop()` 명령어는 안드로이드의 프로세스를 날려버리지만, 현재 펌프와 15초마다 재연결을 시도하는 백그라운드 서비스는 살아있어 '고아 프로세스' 상태가 되며 앱이 꼬이는 현상이었습니다. (최신 안드로이드 정책)
* **해결**: 뒤로가기를 2번 누르면 앱을 강제 종료하지 않고 시스템 표준 방식인 **'앱 최소화(홈 화면으로 내리기)'**로 동작하도록 `FlutterForegroundTask.minimizeApp()`으로 전면 교체했습니다. 이제 다시 앱 아이콘을 누르면 검은 화면 없이 **0.1초 만에 즉시 복구**됩니다.

### 3. '장치 해제' 후 스캔 리스트 먹통 현상 완벽 해결
* **원인**: 웹뷰(JS)에서 '장치 해제'를 눌렀을 때, 백그라운드의 "무한 재연결 타이머"가 죽지 않은 채 스캔 화면으로 돌아가게 되어있었습니다. 블루투스 어댑터가 15초마다 재연결을 시도하느라 사용자가 선택한 새 장치를 연결할 여력이 없었던 것입니다.
* **해결**: `manualDisconnect()`라는 명시적 완전 해제 규격을 신설했습니다. 설정 화면에서 연결 해제를 누르면 영구 저장소의 기기 정보와 무한 루프 타이머가 완벽하게 파괴된 뒤 스캔 화면으로 진입하므로 빠릿빠릿하게 장치를 잡을 수 있습니다.

## ✅ 다음 단계 (검증 요청)
> [!TIP]
> 백그라운드에서 다시 APK 파일을 빌드 중입니다. 완료되면 테스트폰에 새로 설치하신 후, 문제가 발생했던 3가지 시나리오를 다시 한 번만 꼼꼼하게 검증 부탁드립니다!
