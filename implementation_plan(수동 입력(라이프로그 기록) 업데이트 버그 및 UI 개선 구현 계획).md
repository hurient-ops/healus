# 수동 입력(라이프로그 기록) 업데이트 버그 및 UI 개선 구현 계획

수동 입력(혈당, 수면, 스트레스, 특이사항) 시 데이터가 정상적으로 업데이트되지 않는 원인과 측정일시를 공통화하는 UI 개선에 대한 분석 및 구현 계획입니다.

## 원인 분석
1. **업데이트 오류 원인 (`api/log-metrics.ts`)**:
   현재 수면, 스트레스, 특이사항 저장 시 **"사용자의 가장 최근 펌프 로그(1건)"** 를 무조건 가져와서 덮어쓰도록 구현되어 있습니다. 즉, 오늘 펌프 데이터가 아직 동기화되지 않은 상태에서 어제 날짜로 된 로그가 가장 최신이라면, 엉뚱하게 어제 데이터에 덮어써져서 오늘 화면에는 업데이트가 반영되지 않은 것처럼 보이게 됩니다.
2. **UI 구조 문제 (`Dashboard.tsx`, `SampleDashboard.tsx`)**:
   '측정 일시(datetime)' 입력 필드가 '혈당(bg)' 탭 안에만 존재하며, API 통신 시에도 해당 일시 정보가 넘어가지 않아 백엔드에서는 강제로 '현재 서버 시간' 또는 '가장 최근 로그'를 기준으로 처리하고 있었습니다.

## User Review Required

> [!IMPORTANT]
> **펌프 연동 데이터가 없는 날짜에 수동 기록 시 처리 방침**
> 
> 수면/스트레스/특이사항은 `pump_logs`(인슐린 펌프 로그) 테이블에 함께 저장되도록 설계되어 있습니다. 만약 펌프 연동이 안 된 날(펌프 로그가 아예 없는 날)에 수동 기록을 남기려고 하면 어떻게 처리할지 확정이 필요합니다.
> 
> - **옵션 A (권장)**: "해당 날짜의 펌프 연동 데이터가 없어 기록을 추가할 수 없습니다." 라고 에러 메시지를 띄웁니다.
> - **옵션 B**: 펌프 주입량(0)인 빈 껍데기 로그를 억지로라도 강제 생성하여 수동 기록을 넣습니다. (통계 왜곡 가능성 있음)
> 
> 본 계획은 **옵션 A**를 기준으로 구현되었습니다. 승인 시 옵션 A로 적용됩니다.

## Proposed Changes

### 1. 웹 프론트엔드 UI 수정 (공통 측정 일시)

#### [MODIFY] `e:\projects\healus-web\src\pages\Dashboard.tsx`
#### [MODIFY] `e:\projects\healus-web\src\pages\SampleDashboard.tsx`
- 모달 내 '혈당' 탭에만 있던 `<input type="datetime-local">` (측정일시) 폼을 **탭 메뉴 상단으로 끌어올려 모든 탭에서 공통으로 입력**받도록 레이아웃을 수정합니다.
- API 호출(`handleSubmit`) 시, 선택된 `datetime` 값을 `recorded_at` 필드에 담아 서버로 전송하도록 변경합니다.

---

### 2. 백엔드 API 로직 수정 (정확한 날짜 타겟팅)

#### [MODIFY] `e:\projects\healus-web\api\log-metrics.ts`
- 프론트엔드에서 전달한 `recorded_at` 값을 수신합니다.
- **혈당(bg) 기록**: 전달받은 `recorded_at` 날짜를 그대로 `blood_glucose_logs` 에 인서트합니다.
- **수면/스트레스/특이사항(notes)**: `recorded_at`에서 'YYYY-MM-DD' 날짜만 추출한 뒤, `pump_logs`에서 **가장 최근 데이터가 아닌, 해당 날짜(`date`)와 정확히 일치하는 레코드**를 찾아 업데이트합니다.
- 만약 해당 날짜에 생성된 `pump_logs` 데이터가 없다면 클라이언트에게 적절한 에러를 반환하여 알립니다.

## Verification Plan

### Manual Verification
1. **대시보드 페이지 새로고침 후 수동 입력 모달 열기**
   - 모달 최상단에 "측정 일시" 공통 입력 필드가 제대로 노출되는지 확인합니다.
2. **특정 과거 날짜(예: 어제)로 수면/스트레스/특이사항 기록해보기**
   - 기록 완료 시 "저장에 실패했습니다." 에러가 뜨지 않는지 확인합니다.
   - 대시보드의 '라이프로그 요약' 영역 등에서 어제 날짜에 정상적으로 입력된 값이 표시되는지 확인합니다.
