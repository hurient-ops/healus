# BLE 통신 안정성 개선 보고서 (2차 보완 완료)

테스트해주신 결과를 바탕으로 미흡했던 부분들의 원인을 정확히 파악하여 2차 보완을 완료했습니다.

## 🛠️ 추가 보완된 사항

### 1. 통신 단절 시 비정상적인 "주입완료" 모달 발생 방지
* **원인**: 펌프와 통신이 끊어지면 앱이 주입 상태를 `대기(idle)` 상태로 초기화하는데, 앱은 이를 "주입이 정상적으로 완료되었다"고 잘못 해석하여 완료 모달을 띄우고 있었습니다.
* **해결**: 주입 상태가 대기 상태로 변경될 때, **"현재 BLE 연결이 정상적으로 유지되고 있는지"**를 먼저 검증하도록 방어 코드를 추가했습니다. 이제 주입 도중 연결이 끊어져서 대기 상태로 돌아가는 경우에는 완료 모달 대신 "통신 단절로 인해 주입 상태를 알 수 없습니다"라는 안내 메시지(Toast)만 띄웁니다.

### 2. 멀리 떨어졌을 때의 "자동 재연결" 성공률 대폭 향상 (20% -> 100% 목표)
* **원인**: 기존 코드는 기기가 멀리 떨어져 있을 때 15초마다 강제로 연결을 시도하고 실패하면 에러를 뱉는 '폴링(Polling) 타이머' 방식이었습니다. 안드로이드는 기기가 범위에 없을 때 계속해서 수동 연결(Direct Connect)을 시도하면 블루투스 스택이 꼬이거나 에러(133)를 발생시킵니다.
* **해결**: 타이머 방식을 폐기하고, 안드로이드/iOS 운영체제의 네이티브 백그라운드 연결 대기 기능인 **`autoConnect: true`** 모드로 전면 개편했습니다. 
  이제 펌프가 범위 밖으로 나가 연결이 끊어지면, 앱은 에러를 내뿜지 않고 운영체제에게 "저장된 MAC 주소가 보이면 즉시 연결해줘"라고 요청한 채 백그라운드에서 조용히 무한 대기합니다. 펌프 곁으로 다가오면 운영체제가 즉각적으로 인지하여 자동으로 연결을 복구하므로, 성공률이 획기적으로 높아질 것입니다.

### 3. 장치 해제 후 리스트 먹통 현상 & 시스템 뒤로가기 꼬임 완벽 해결
* **원인**: 설정에서 "연결 해제"를 눌러 장치를 끊더라도, 앱 내부의 **"최초 핸드셰이크(보안 인증) 성공" 캐시**가 초기화되지 않고 남아있었습니다. 그래서 스캔 리스트에서 장치를 눌러도 이미 성공했다고 착각하고 다음 동작을 수행하지 않았습니다. 또한 시스템 뒤로가기 제어가 꼬여 있었습니다.
* **해결**: 연결 해제 시 모든 보안 인증 성공 상태를 강제로 초기화(`reset`)하고, 웹뷰의 히스토리도 날리도록 수정했습니다. 또한, 장치 스캔 화면과 대시보드 화면에서는 시스템 뒤로가기를 누를 시 이전 화면으로 돌아가지 않고 **앱을 홈 화면으로 최소화**하도록 통일하여 흐름이 꼬이는 문제를 원천 차단했습니다.

## ✅ 다음 단계 (검증 요청)
> [!TIP]
> 백그라운드에서 다시 APK 파일을 빌드 중입니다. 완료되면 테스트폰에 새로 설치하신 후, 문제가 발생했던 부분을 한 번 더 검증 부탁드립니다!
