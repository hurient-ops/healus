# 목표

사용자 피드백에 따른 프론트엔드 UI/UX 대대적 고도화 및 백엔드 데이터(회식, 운동 모드) 확장 구현

## 제안하는 변경 사항

### 1. 프론트엔드 라우팅 및 레이아웃 수정 (`healus-web`)
#### [MODIFY] `src/pages/Home.tsx`
- 랜딩 페이지 우측 상단 GNB 영역에 "회원가입" 아이콘/버튼 추가.
- 전체적인 여백, 배경 그라데이션, 카드 UI 등을 더욱 미려하게 리디자인.
#### [NEW] `src/pages/Signup.tsx`
- 회원가입 더미 페이지 추가 (디자인 통일감 유지).
#### [MODIFY] `src/pages/Diabetes.tsx`
- 기존 좌측 목차(LNB)와 하단 이전/다음 글 영역 제거.
- **파스타몰 가이드**를 참조하여 '1형 당뇨', '2형 당뇨', '임신 당뇨', '당뇨병 전단계'에 대한 심도 있는 내용 추가.
#### [MODIFY] `src/pages/Diet.tsx`, `src/pages/PumpGuide.tsx`, `src/pages/Complications.tsx`
- 참조 사이트의 풍부한 가이드라인을 바탕으로 아티클 내용을 대폭 확장.
- 단순 텍스트 나열을 피하고, 카드 뷰, 하이라이트 박스 등을 활용하여 예쁘게 스타일링.

### 2. 백엔드 AI 모델 점검 (`healus-backend`)
- LM Studio에 로드된 모델명(`llama-3.1-storm-8b`)은 현재 일치합니다. 기존 테스트 스크립트에서 발생한 문제는 Windows 콘솔 인코딩(cp949) 문제로 판명되었습니다. 
- API 실행 시 예외가 발생하지 않도록 `api/web.py`의 예외 처리를 더욱 강화하고, 프롬프트에 '회식' 및 '운동' 데이터를 추가로 주입하여 더 정밀한 AI 통찰력을 제공하도록 개선합니다.

### 3. 데이터베이스 확장 및 시드 100개 생성 (`healus-backend`)
#### [MODIFY] `models/models.py`, `models/schemas.py`
- `PumpLog` 테이블에 `dining_out_mode` (회식), `exercise_mode` (운동) 추가
#### [MODIFY] `seed_data.py`
- 기존 DB를 초기화하고 **100일 치(100개)**의 펌프 로그와 혈당 로그 생성. 

### 4. 대시보드 UI 반영 (`healus-web`)
#### [MODIFY] `src/pages/Dashboard.tsx`
- 백엔드로부터 100일치 데이터를 받아와 차트에 스크롤 형태로 모두 렌더링.
- 펌프 주입량(식사, 기초, 추가) 트렌드 차트에 **[회식 🍔]**, **[운동 🏃‍♂️]** 뱃지/아이콘을 표시.

---

## 🚨 오픈 퀘스천: 앱 수정 없이 데이터를 수집하는 방법

Healus 앱을 수정하지 않으신다면, 대시보드에서 '회식'과 '운동'을 처리하기 위해 **다음 두 가지 방법 중 하나를 선택**해야 합니다.

### [방법 A] 웹 대시보드에서 수동으로 입력하게 만들기 (추천)
- 기존에 만들어둔 **[혈당 수동 입력]** 버튼처럼, 웹 대시보드 상단에 **[오늘 회식했어요]**, **[오늘 운동했어요]** 버튼을 만듭니다.
- 사용자가 웹사이트(healus-web)에 접속해서 클릭하면 DB에 반영되고 차트에 뱃지가 뜹니다.

### [방법 B] 데이터 패턴을 분석해서 AI가 자동으로 유추하게 만들기
- 사용자가 버튼을 누르지 않아도, 백엔드 로직이 수치를 보고 **자동 태깅**합니다.
- 예: 오늘 식사 주입량(Bolus)이 최근 7일 평균보다 30% 이상 급증했다면 -> 시스템이 자동으로 **[회식 의심 🍔]** 태그를 붙임.
- 예: 오늘 기초 주입량(Basal)이 일시 정지되거나 크게 줄었다면 -> 시스템이 자동으로 **[운동 의심 🏃‍♂️]** 태그를 붙임.

어떤 방법으로 구현하는 것이 기획 의도에 맞으실까요? 방법 A나 B 중 하나를 선택해 주시거나 자유롭게 의견을 말씀해 주세요!
