# 대시보드 정렬 및 데이터 중복 생성 해결 계획

## 목표 
펌프 앱에서 대량의 과거 데이터(Bulk Logs)를 동기화할 때 중복 날짜 데이터가 생성되는 문제를 해결하고, Vercel 대시보드에서 날짜가 뒤죽박죽으로 표시되는 차트 정렬 문제를 수정합니다.

1. **중복 데이터 문제**: 앱에서 대량 데이터가 올라올 때, 백엔드 서버는 `pump_id`, `월(month)`, `일(day)`이 모두 일치하는 데이터가 있는지만 검사합니다. SQL 스크립트로 임의로 넣으신 기존 테스트 데이터는 `pump_id`가 다르게 들어있었기 때문에, 서버가 기존 데이터를 찾지 못하고 같은 날짜의 빈 데이터를 중복으로 생성해버렸습니다.
2. **차트 정렬 문제**: 현재 대시보드 코드는 '실제 날짜(`date`)'가 아닌 '데이터가 삽입된 시간(`created_at`)'을 기준으로 정렬하여 상위 100개를 가져옵니다. 대량 데이터가 한 번에 쏟아져 들어오면서 순서가 어긋난 채로 렌더링되고 있습니다.

## 수정 계획 (Proposed Changes)

### healus-backend
#### [MODIFY] [logs.py](file:///e:/projects/healus-backend/api/logs.py)
- 기존 데이터를 찾는 조건(Query)을 수정하여, `pump_id`가 다르더라도 해당 **`user_id`와 `월(month)`, `일(day)`이 같다면** 무조건 기존 테스트 데이터를 찾아내어 덮어쓰기(Update) 하도록 로직을 수정합니다.
- 기존 SQL 테스트 데이터에 `pump_id`가 빠져있었다면, 덮어쓰기할 때 앱에서 올라온 실제 `pump_id`로 정보를 채워 넣습니다.

### healus-web
#### [MODIFY] [dashboard.ts](file:///e:/projects/healus-web/api/dashboard.ts)
- Supabase에서 데이터를 가져올 때 `.order('created_at')` 대신 **`.order('date', { ascending: false })`**를 사용하도록 변경합니다. 이렇게 하면 데이터 삽입 시간에 구애받지 않고 무조건 역순 날짜대로 차트가 예쁘게 정렬됩니다.

## 검증 계획 (Verification Plan)
### 데이터베이스 정리 및 수동 검증
- 위 소스 코드를 수정한 뒤 백엔드 서버에 1회성 정리 스크립트를 실행시켜서, 이미 DB에 잘못 생성되어버린 '0.0짜리 중복 쓰레기 데이터'들을 깔끔하게 삭제(Cleanup)하겠습니다.
- 불필요한 데이터가 삭제되고 쿼리가 수정되면 대시보드 새로고침 시 즉시 날짜가 정상 순서대로 렌더링될 것입니다.
