# 목표

사용자님의 5가지 불만 사항을 완벽하게 해결하기 위한 프론트엔드 전면 리뉴얼 및 백엔드 정상화 계획서입니다.

## 문제 원인 파악 및 해결책

1. **대시보드 가상 데이터 미출력 원인**: 지난번 제가 만들어드린 자동 실행 스크립트(`run_healus.bat`)에 오타가 있어 백엔드 서버가 켜지자마자 꺼지는 문제가 있었습니다. 이로 인해 대시보드가 백엔드와 연결되지 못해 텅 비어 보였습니다. 스크립트를 즉시 수정하여 100일 치 가상 데이터가 정상적으로 쏟아지도록 고치겠습니다.
2. **랜딩 페이지(`Home.tsx`) 디자인 빈약**: 단순히 상단 메뉴만 바꾼 것을 넘어, 메인 랜딩 페이지 자체를 최신 트렌드에 맞게 완전히 뜯어고치겠습니다. (커다란 배너, 소개 섹션, 애니메이션 등 추가)
3. **서브 4개 페이지 가로폭 및 이미지 부족**: 가로폭 제한을 넓히고(max-w-7xl), 관련된 고품질 이미지를 페이지 중간중간 듬뿍 배치하여 훨씬 풍성해 보이도록 디자인을 확장하겠습니다.
4. **상단 메뉴(GNB) 수정**: 눕혀 쓰기(Italic)를 제거하고, 정자체(Gmarket Sans 굵은 글씨)로 바꿉니다. "<- 메인으로 돌아가기" 글씨를 없애고 그 자리에 'Healus' 로고를 배치하며, 누르면 홈으로 가도록 직관적으로 수정합니다.
5. **AI 추론 상세 표시**: 대시보드에 표시되는 AI 분석 결과(Insight) 영역을 훨씬 크고 화려하게 키우고, AI가 어떤 근거(최근 14일 식사/기초 주입량 등)로 추론을 했는지 로딩 과정과 근거를 UI에 명확하게 노출하겠습니다.

## 상세 작업 계획 (Proposed Changes)

### 1. 백엔드 구동 스크립트 수정
#### [MODIFY] `E:\projects\run_healus.bat`
- 백엔드 실행 명령어 오타 수정 (`api.main:app` -> `main:app`)

### 2. 레이아웃 및 GNB 전역 수정
#### [MODIFY] `src/pages/*.tsx` (전체 6개 페이지)
- 모든 페이지의 상단 헤더에서 "<- 메인" 제거.
- 왼쪽 최상단에 똑바른 정자체의 **Healus** 로고 배치 및 홈 링크(`/`) 연결.

### 3. 랜딩 페이지 전면 개편
#### [MODIFY] `src/pages/Home.tsx`
- 커다란 Hero 배너 이미지 적용.
- 서비스 특장점(Smart Dashboard, AI Insight 등)을 보여주는 섹션 3개 추가.
- 하단 푸터(Footer) 추가.

### 4. 4가지 서비스 화면 가로폭 확장 및 이미지 추가
#### [MODIFY] `src/pages/Diabetes.tsx`, `Diet.tsx`, `PumpGuide.tsx`, `Complications.tsx`
- 컨테이너 폭을 넓혀 꽉 찬 느낌을 주도록 레이아웃 변경.
- 섹션마다 관련 이미지 2~3장씩 대폭 추가.

### 5. AI 추론 내역 상세 표시
#### [MODIFY] `src/pages/Dashboard.tsx`
- AI Insight 박스를 대시보드 최상단으로 올리고 화려하게 강조.
- "AI가 14일간의 데이터를 바탕으로 추론 중입니다..." 같은 로딩 UI 추가.
- "추론 근거: 평일 평균 X, 주말 평균 Y" 등 AI가 학습 및 추론에 사용한 데이터를 사용자에게 투명하게 노출.

---

> [!IMPORTANT]
> 백엔드가 켜지지 않아 대시보드가 비어 있었던 점 정말 죄송합니다. 이번에는 화면 디자인을 완전히 갈아엎어서 '예쁘고 풍성하게' 만들어 드리겠습니다. 
> 이 계획대로 전면 수정을 진행할까요? 동의하시면 **Proceed** 버튼을 눌러주세요!
