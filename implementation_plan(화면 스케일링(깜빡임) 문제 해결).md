# 화면 스케일링(깜빡임) 문제 해결 계획

현재 웹뷰(WebView)가 나타날 때 화면이 작았다가 커지는(스케일링) 현상은 **Flutter 웹뷰 엔진이 초기화되어 화면에 렌더링을 시작하는 시점**과 **내부 HTML 파일이 `<meta name="viewport">` 태그를 파싱하여 모바일 해상도에 맞게 레이아웃을 다시 그리는 시점** 간의 미세한 시간 차이 때문에 발생합니다. 

이러한 "렌더링 시점 차이(Flickering/Jank)"를 해결하는 **가장 좋은이자 업계 표준 방법은 "Fade-in(페이드인) 전환 기법"**을 사용하는 것입니다.

## 가장 좋은 해결 방법: 페이드인 (Fade-in) 전환 기법
웹뷰가 내부적으로 HTML 로딩과 사이즈 계산을 완전히 끝마칠 때까지는 화면에 투명하게(안 보이게) 숨겨두고, 로딩이 완료된 직후 부드럽게 화면에 나타나게(Opacity 0 -> 1) 처리하는 방식입니다.

## 적용 과정

### 1. Flutter 상태 관리 추가
`main.dart`에 웹뷰 로딩 완료 여부를 추적하는 상태 변수(`_isWebViewLoaded`)를 추가합니다.

### 2. 웹뷰 컨트롤러 이벤트 연동
웹뷰 컨트롤러의 `onPageFinished` 콜백(웹뷰 내 모든 HTML 및 Viewport 계산 완료 시점)이 호출될 때 `_isWebViewLoaded = true`로 변경하여 렌더링 완료를 알립니다.

### 3. AnimatedOpacity 위젯 적용
화면을 그리는 `WebViewWidget`을 `AnimatedOpacity` 위젯으로 감싸서 로딩 중에는 투명도(Opacity)를 0으로 숨겨두고, 완료 시 1로 부드럽게(약 300ms) 전환되도록 수정합니다.

---
## User Review Required

> [!NOTE]
> 위 방식으로 적용하면, 사용자는 깜빡이거나 스케일링되는 렌더링 과정을 전혀 볼 수 없으며, 앱이 아주 부드럽게 로딩되는 프리미엄(Premium)한 경험을 느끼게 됩니다. 
> 
> **이 해결 방법을 코드에 즉시 반영할까요? 하단의 'Proceed' 버튼을 눌러 승인해 주시면 바로 코드를 수정하겠습니다!**
