# BLE 통신 흐름 제어 및 시간 동기화 수정 완료

BLE 통신 중 여러 패킷이 비동기적으로 혼재되어 전송되는 문제를 해결하고, 시간 동기화(0x06) 로직을 최적화하는 작업을 성공적으로 완료했습니다! 🎉

## 수정 사항 요약

1. **전역 큐 및 락(`BleMutex`) 도입**
   - 기존에는 각각의 컨트롤러가 `sendPacket`을 호출하면 바로 패킷을 펌프에 전송했습니다.
   - 새롭게 `ble_mutex.dart`를 도입하여, **"요청 패킷 전송 -> 올바른 응답 패킷 수신 대기 -> 완료"**의 1:1 Flow가 끝날 때까지 다른 명령어들이 순차적으로 큐(Queue)에서 대기하도록 변경했습니다.
   - 기존의 파싱 및 화면 업데이트 로직은 **100% 원본 그대로 유지**됩니다.

2. **명령어별 정확한 응답 대기 매핑**
   - 패킷을 보낸 후 단순히 `0x00`(BT_MSG_RES)만 기다리던 로직을 고도화했습니다.
   - 펌프 고유 PID(`0x09`)를 보내면 정확히 `0x0A` 응답을, 배터리 잔량(`0x70`)을 보내면 정확히 `0x72` 응답을, 암호 확인(`0x41`)을 보내면 `0x42` 응답을 수신할 때까지 기다리도록 1:1 매핑하였습니다.
   - 응답 전용 코드가 없는 일반 패킷만 `0x00`을 대기합니다.

3. **시간 동기화(0x06) 중복 전송 해결**
   - 시간 동기화(0x06) 전송 시 타임아웃을 500ms에서 넉넉히 **3초**로 늘렸습니다. (로그 상 펌프의 0x44 응답이 오는데 1.2초 소요됨)
   - 이제 타임아웃 전에 `0x44`를 수신하여 시간을 대조하고 일치하면 `상태=완료` 처리되어, 재시도 타이머 및 중복 함수 호출 시 **시간 동기화 패킷이 더 이상 연속으로 전송되지 않습니다.**

## 검증 결과

- 모든 코드를 수정한 뒤 `flutter build apk --debug` 빌드를 수행하여 **성공적으로 APK 빌드가 완료**되었습니다.
- 컴파일 에러 없이 정상적으로 빌드되었으므로 앱 실행 시 안정적으로 작동할 것입니다.

> [!TIP]
> 이제 실제 펌프(또는 모의기기)와 연결하여 송수신 로그(0xEF로 시작)를 살펴보시면, `0x06` 전송 후 `0x44` 수신이 끝난 뒤에만 `0x70`이 전송되는 깔끔한 1:1 순차적 흐름을 보실 수 있을 것입니다!

앱을 폰이나 에뮬레이터에 설치하여 실제 로그를 테스트해 보시기 바랍니다. 추가적으로 더 수정이 필요한 부분이 있다면 언제든 말씀해 주세요!
