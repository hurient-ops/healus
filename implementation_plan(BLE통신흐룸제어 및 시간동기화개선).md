# BLE 통신 흐름 제어 및 시간동기화 개선

BLE 통신 중 패킷 송수신 순서가 꼬이는 문제(하나의 송수신 Flow가 완료되기 전에 다음 패킷이 전송되는 현상)와 시간동기화 패킷이 지속적으로 중복 송신되는 문제를 해결합니다.

## User Review Required

> [!WARNING]
> 현재 모든 BLE 패킷 전송을 동기화하기 위해 앱 전역에 `BleMutex`(순차적 송수신 제어 락)를 도입할 계획입니다. 이렇게 되면 이전처럼 비동기로 다수의 패킷이 동시에 보내지는 것이 막히고, `요청 송신 -> 응답 수신 대기 -> 다음 요청 송신` 순서대로 하나씩 큐에서 처리됩니다. 앱의 다른 기능(펌프의 다른 데이터 파싱 등)은 100% 원본 코드를 유지합니다.

## Open Questions

> [!IMPORTANT]
> 인슐린 주입(`0x17`)과 같은 특수 명령의 경우 응답으로 `0x00` 수신 직후부터 장시간 주입이 진행되며 최종적으로 `0x3B`(주입 완료)가 수신됩니다. 큐에서 대기할 때 `0x00`(정상 수신 응답)까지만 대기하고 다음 명령을 보낼 수 있게 할지, 아니면 주입이 다 끝날 때(`0x3B`)까지 전체 큐를 대기시켜야 할지 결정이 필요합니다. 현재 기기의 `idle` 상태 체크 로직이 별도로 있으므로, 큐는 `0x00` 응답(또는 각각의 1차 응답)까지만 대기하고 넘어가도록 설계하겠습니다. 동의하시나요?

## Proposed Changes

### `lib/services/ble/ble_mutex.dart`
#### [NEW] `ble_mutex.dart`
BLE 명령이 전송되고 응답을 받을 때까지 다른 전송을 차단하여 순서를 보장하는 전역 큐 락(`BleMutex`)을 구현합니다.

---
### `lib/services/inject/inject_controller.dart`
#### [MODIFY] `inject_controller.dart`
- 큐 처리 로직(`_processQueue`) 내부에 `BleMutex`를 적용합니다.
- 패킷 송신 후 무조건 `0x00`만 대기하는 것이 아니라, 송신하는 Opcode에 맞는 올바른 응답(예: 시간 동기화 `0x06` 송신 시 `0x44` 수신, 배터리 `0x70` 송신 시 `0x72` 수신, 패스워드 `0x41` 송신 시 `0x42` 수신)을 매핑하여 대기하도록 로직을 추가합니다. 이렇게 하면 `0x00` 없이 바로 전용 응답 패킷이 오는 경우에도 완벽한 1:1 Flow를 보장합니다.

---
### `lib/services/ble/time_sync_controller.dart`
#### [MODIFY] `time_sync_controller.dart`
- 시간 동기화(0x06) 전송 전 `BleMutex`를 획득하여 다른 패킷과 겹치지 않게 합니다.
- 기존의 500ms 타임아웃을 3초로 늘려, 펌프 응답(0x44)이 오기도 전에 타임아웃되어 다시 송신하는 문제를 해결합니다.
- 시간 비교 로직(`timeBytes[i] != resBytes[i]`)이 정상 통과하면 상태를 완료(`state = true`)로 변경하고, 다시는 0x06이 송신되지 않도록 타이머와 중복 호출을 확실하게 차단합니다.

## Verification Plan

### Automated Tests
- 없음

### Manual Verification
1. 앱 빌드 후 인슐린 펌프(또는 모의 장치)와 연결.
2. ADB 로그나 `sentPacketsLogProvider` 로그를 통해 패킷이 송신된 후, 해당 응답 패킷이 수신 완료된 시점 이후에만 다음 패킷이 송신되는지 시퀀스 확인.
3. 최초 0x06 송신 후 0x44 수신 시 정확히 송수신이 한 번만 일어나고 더 이상 0x06이 반복 전송되지 않는지 확인.
