# 기기 연동 방식 변경 마이그레이션 결과

모바일 앱-웹 간 원활한 기기 데이터 동기화를 위해, 계정 생성 시 기기 식별자(`PID`)와 부가 정보를 필수로 수집하도록 시스템을 개편했습니다.

## 변경 내용 (Changes)

1. **백엔드 스키마 및 DB 마이그레이션**
   - 사용자(`User`), 펌프 로그(`PumpLog`), 블루투스 패킷(`RawPacketLog`) 모델에서 기존 `device_mac` 필드를 모두 `pump_id`로 변경했습니다.
   - 사용자 모델에 `phone_number`(전화번호), `birth_date`(생년월일), `terms_agreed`(동의여부) 필수 필드를 추가했습니다.
   - 기존의 `healus.db`를 삭제하고 새로운 스키마를 반영하여 재생성 및 초기 데이터(Seed)를 구성했습니다. 가상의 16byte PID(`1234567890ABCDEF1234567890ABCDEF`)가 연동되도록 조치했습니다.

2. **API 업데이트**
   - 회원가입(`POST /signup`) 시 신규 필드를 데이터베이스에 저장하도록 검증 및 삽입 로직을 변경했습니다.
   - 펌프 로그 조회 및 동기화 API에서 `pump_id` 기반으로 사용자를 맵핑하도록 식별자 조회 로직을 갱신했습니다.

3. **프론트엔드 UI/UX (Signup.tsx)**
   - 이름과 생년월일, 핸드폰 번호와 이메일을 나란히 배치하여 공간 활용도와 가독성을 높였습니다.
   - 기기 PID 입력창을 필수값으로 설정하고, 모바일 앱과의 연동을 위해 왜 이 값이 필요한지에 대한 안내 문구를 추가했습니다.
   - "개인정보 및 민감정보 수집 및 이용" 동의 체크박스를 추가했으며, 해당 약관에 동의해야만 '계정 만들기' 버튼이 활성화되도록 제어 로직(`disabled={!agreed}`)을 구현했습니다.

## Validation Results

- **백엔드 DB 재생성 완료**: `app.db` -> `healus.db`로 교체되어 초기화가 원활히 완료되었으며, Swagger 명세도 변경된 Pydantic Schema에 따라 자동 업데이트되었습니다.
- **Vite 빌드 통과**: 프론트엔드의 `npm run build` 결과 타깃 코드(`Signup.tsx`)에서 런타임 오류나 컴파일 에러가 발견되지 않았습니다.
- **UI 화면 확인**:

![개편된 회원가입 폼](/C:/Users/COMPANY/.gemini/antigravity-ide/brain/38d0f638-958b-417f-a567-c6f7af8ad78f/signup_form_fields_1784984740307.png)

> [!TIP]
> 이제 Healus 웹 대시보드에서 가입한 계정과, 동일한 PID가 삽입된 인슐린 펌프를 컨트롤하는 모바일 앱이 유기적으로 연동되어 데이터를 주고받을 수 있는 기반이 마련되었습니다.
