# 전문 BI 대시보드(KPI) 형태 전면 개편 계획

첨부해주신 이미지(Financial KPI Dashboard / Insurance Dashboard)를 확인했습니다! 사용자님께서 원하시는 것은 현재의 단순한 리스트나 커스텀 막대그래프 형태가 아니라, **실제 기업이나 전문가용 모니터링 시스템에서 사용하는 고도화된(BI) 차트 레이아웃**이라는 점을 정확히 파악했습니다.

이를 위해 리액트 전문 차트 라이브러리인 `recharts`를 도입하고, 대시보드의 화면 분할과 데이터 시각화 방식을 완전히 갈아엎겠습니다.

## Open Questions (사용자님께 여쭤볼 점)

> [!IMPORTANT]
> 첨부해주신 이미지 2장 중 하나는 **밝고 깔끔한 화이트톤(재무 KPI)**이고, 다른 하나는 **어둡고 화려한 다크톤(보험 대시보드)**입니다. 
> 현재 Healus 웹은 전체적으로 밝은 톤(화이트/라이트그레이)을 유지하고 있어 **첫 번째 이미지(화이트톤)** 스타일로 레이아웃을 쪼개고 차트를 구성하는 것을 추천해 드립니다. 만약 두 번째 이미지처럼 완전한 **다크 모드(검은 배경 + 네온 차트)**를 원하신다면 피드백으로 말씀해주세요!

## Proposed Changes (변경 상세 내용)

### 1. 차트 라이브러리 추가 및 레이아웃 전면 개편
- **[NEW] `recharts` 패키지 설치**: 복잡한 원형 게이지, 멀티 라인 차트, 스캐터(분산) 차트를 아름답게 그리기 위해 업계 표준 라이브러리를 도입합니다.
- **`src/pages/Dashboard.tsx` 3단 그리드 개편**:
  - **상단 (Top KPI Row)**: 목표 혈당 달성률(%), 평균 혈당 수치, 평균 기초 주입량, 평균 식사 주입량 4가지 원형 게이지 차트 배치. (배경에 옅은 실사 이미지를 깔아 대시보드도 예쁘게 꾸밉니다.)
  - **중단 (Middle Row)**: 좌측엔 최근 요약 테이블, 우측엔 30일 혈당 및 주입량 겹침 라인 차트(Trend Line Chart) 배치.
  - **하단 (Bottom Row)**: 좌측엔 AI 소견 텍스트 카드, 우측엔 오류 빈도/종류 바(Bar) 차트 배치.

### 2. 가상 데이터(Mock Data) 대폭 확장 및 완전 무작위 이벤트 부여 (옵션 B 고도화)
- **`healus-backend/scripts/generate_mock_data.py` 및 백엔드 스키마 수정**: 
  - 단순히 펌프 주입량만 있는 것이 아니라, **혈당 수치(CGM), 식사 탄수화물 양, 수면 시간, 스트레스 지수, 운동 시간** 등 다채로운 라이프로그(Lifelog) 데이터를 생성하도록 백엔드 데이터를 고도화합니다.
  - **이벤트(회식, 운동 등)의 무작위 생성**: 평일/주말이라는 단순한 기준을 없애고, 요일에 상관없이 랜덤한 확률로 '회식(고탄수화물 섭취 및 음주)', '야근(스트레스 및 수면부족)', '운동(혈당 감소 및 민감도 증가)' 이벤트가 발생하도록 데이터를 섞어서 생성합니다.
  - 이를 통해 AI가 요일에 얽매이지 않고 "수요일과 목요일에 회식이 있었고, 그로 인해 수면이 부족해져 다음 날 인슐린 저항성이 커졌다"는 등 Healus 앱 실데이터 기반의 복합적이고 진짜 같은 분석 결과를 도출하도록 만듭니다.

### 3. AI 연동 가시성 극대화 및 데이터 소팅 (Transparent AI)
- **AI 분석 과정 표출**: 단순히 결과만 보여주는 것이 아니라, 대시보드 화면 한편에 "AI가 현재 분석 중인 데이터(수면 부족, 주말 식사량 증가 등)"와 "어떤 모델을 사용해 어떤 프롬프트로 도출했는지"를 명확하게 시각화하여 표출합니다.
- **앱 데이터 소팅 및 정밀 분석**: Healus 모바일 앱에서 수집된 방대한 라이프로그 데이터를 백엔드에서 정밀하게 소팅(Sorting) 및 필터링하여 대시보드 차트에 겹쳐 그리고 분석 결과를 도출합니다.

### 4. 랜딩 페이지 자동 슬라이드(Carousel) 적용 및 헤더 버튼 개선
- **`src/pages/Home.tsx` 수정**: 파스타(PastaHealth) 메인 화면처럼, 랜딩 페이지 최상단 히어로 배너가 일정 시간마다 텍스트와 배경 실사 이미지가 자동으로 넘어가는(Carousel) 화려한 애니메이션 화면으로 만듭니다.
- **헤더 우측 버튼 스타일 수정**: 반전되어 어색했던 '로그인' 버튼을 '회원가입' 버튼과 동일한 톤으로 맞추고, 두 버튼 모두 모서리가 둥근 박스(Pill 스타일) 형태로 통일성 있게 디자인합니다.

### 5. 간편 회원가입(SNS 로그인) 및 펌프 기기 연동 정보 추가
- **`src/pages/Signup.tsx` 수정**: 
  - 요즘 트렌드에 맞게 **카카오, 네이버, 구글 간편 로그인/회원가입 버튼**을 화려하고 깔끔하게 추가합니다.
  - 회원가입 시 단순히 이메일/비밀번호만 받는 것이 아니라, **현재 사용 중인 인슐린 펌프 기종 선택** 및 펌프 특징을 연계할 수 있는 입력폼을 추가하여 향후 Healus 모바일 앱과의 연동성을 확보합니다.

### 6. 전 페이지 '실사 이미지' 융단폭격 및 비회원 가이드 리뉴얼
- **모든 페이지(All Pages) 시각화 극대화**: 랜딩 페이지, 회원가입/로그인, 대시보드, 그리고 비회원 가이드 4종 등 **모든 웹페이지 화면 곳곳에 고품질 실사 이미지를 최대한 꽉꽉 채워 넣습니다.** 화면이 전환되거나 스크롤을 내릴 때마다 관련 이미지가 눈을 즐겁게 하도록 구성합니다.
- **`src/pages/Diabetes.tsx` 등 가이드 4종 수정**:
  - 파스타(PastaHealth)의 1형 당뇨 가이드 페이지의 깔끔하고 정보량이 풍부한 구조를 벤치마킹하여 텍스트를 대폭 보강합니다.
  - **100% 리얼 실사(Photorealistic) 이미지 대폭 추가**: AI 느낌이 나는 일러스트는 절대 쓰지 않습니다! Unsplash 등에서 가져온 **진짜 사람, 실제 의료기기, 건강한 식단의 고해상도 실사 사진**을 대폭(기존 대비 3배 이상) 투입하여 신뢰감을 극대화합니다.

---

## Verification Plan
1. 패키지 설치 후 `npm run dev` 구동 재확인.
2. 가짜(Mock) 데이터를 `recharts` 포맷에 맞게 변환하는 로직 테스트.
3. 웹페이지 접속 시 다양한 화면 크기에서도 차트들이 첨부된 이미지처럼 꽉 차고 예쁘게 그리드로 정렬되는지(Responsive Grid) 검증.

> [!TIP]
> 위 내용(화이트톤 기반의 전문 BI 레이아웃)으로 대시보드를 180도 바꾸는 것에 동의하시면 **Proceed** 버튼을 눌러주세요! 다크톤을 원하시거나 다른 의견이 있으시면 텍스트로 남겨주시면 됩니다.
