# 주입 전 상태 확인 로직 구현 완료

요청하신 **"주입 전 기기의 idle 상태 선행 확인 로직"** 구현을 성공적으로 마쳤습니다.

## 🛠️ 변경 및 적용 사항

1. **불필요해진 기존 로직 제거 (`ble_handshake_controller.dart`)**
   - 기존의 `connectAndHandshake`에서 0x04 패킷을 보내고 대기하던 불필요한 상태 확인 코드를 모두 제거했습니다.

2. **비동기 상태 확인 메서드 추가 (`inject_controller.dart`)**
   - 주입 전 기기 상태를 먼저 점검하고 패킷 송출을 지연시키거나 취소시키는 `checkStateAndExecute` 메서드를 새롭게 구현했습니다.
   - 송신(`0x04`) 후 비동기 대기하며 `RES(0x00)` 수신, 이어서 `IND(0x04/0x05)` 수신 시 상태값이 `idle(0x01)`일 때에만 큐에 목표 패킷을 추가합니다.

3. **WebView 핸들러 적용 (`main.dart`)**
   - 식사 주입, 추가 주입, 회식 적용, 운동 적용 부분에서 기존 `queuePacket()` 직접 호출 방식을 모두 방금 생성한 `checkStateAndExecute()` 호출로 교체하였습니다.

4. **전역 UI 알림(Toast 대체) 추가 (`globals.dart`)**
   - 상태 불량이나 타임아웃 발생 시, 화면 하단에 안전하게 경고 스낵바를 노출하기 위해 `rootScaffoldMessengerKey`를 도입하고 "주입 요청을 취소합니다." 문구가 노출되도록 구성했습니다.

5. **테스트 편의성 (Timeout Hold) 연동**
   - '가상 BLE 수동 제어 패널'의 `timeoutHoldProvider` 값이 true일 경우, 응답 대기 시간이 무한대(365일)로 설정되어 개발자가 원할 때 수동 패킷을 송신해 테스트할 수 있도록 적용했습니다.

---

### 검증 플랜
- 앱을 구동하신 뒤, "타임아웃 일시정지" 옵션을 켠 상태에서 주입 명령을 시도해 보세요.
- 앱이 멈춘 상태에서 수동 제어 패널로 `0x00` -> `0x05 (idle)`을 보내 정상 처리되는지 확인하고, 타임아웃을 끈 채로 3초간 응답이 없을 때 경고창이 뜨는지 확인하시면 됩니다!
