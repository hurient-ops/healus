# 대시보드 클라우드 이식, 모바일 최적화 및 수동 입력 확장 계획

로컬 환경에 의존하고 있는 대시보드를 Vercel 클라우드 및 Supabase 실제 DB로 완벽하게 이전하고, 모바일 반응형을 적용하며, 사용자가 수면시간과 스트레스 지수를 직접 입력할 수 있는 기능을 추가합니다.

## User Review Required

> [!NOTE]
> **진행 예정인 핵심 작업 요약**
> 1. **실제 DB 구축 및 100일 치 가상 데이터 세팅**: Supabase 클라우드에 실제 데이터를 저장할 테이블을 만들고 100일 치 가상 데이터(운동, 회식 데이터 포함)를 넣는 SQL 코드를 제공합니다.
> 2. **AI 대시보드 서버리스 이식**: 파이썬 서버에 있던 분석 로직을 Vercel(클라우드 API) + Groq API(`llama-3.3-70b`)로 완전히 옮깁니다.
> 3. **스마트폰 반응형 UI 최적화**: Tailwind CSS 모바일용 클래스를 추가하여 스마트폰 화면에 맞게 재배열합니다.
> 4. **수동 기록 기능 확장 (DB 연동)**: 기존 '혈당 수동 입력' 팝업 창을 3가지 탭(혈당/수면시간/스트레스)으로 확장하고 DB에 즉시 저장되도록 연동합니다.
> 5. **(신규) 라이프로그 자동 표출 UI**: 펌프(TX/RX)에서 파싱된 '운동 시간'과 '이벤트 태그(회식 등)'를 대시보드 메인 화면의 특정 위치에 보기 좋게 표출합니다.

## Proposed Changes

---

### 1. Database (Supabase) Setup

#### [NEW] `supabase_schema_and_data.sql` (작업 후 코드 제공 예정)
- `pump_logs`, `blood_glucose_logs` 테이블 생성 및 100일 치 가상 데이터 일괄 삽입
- **(추가) 삽입되는 100일 치 가상 데이터에 랜덤으로 펌프 기기에서 추출되었다고 가정된 '운동 시간' 및 '이벤트(회식 등)' 데이터를 명확하게 포함시킵니다.**

---

### 2. Vercel Serverless API (신규 API 라우트)

#### [NEW] `api/dashboard.ts` 
- 클라우드 DB에서 100일 치 데이터 및 '운동 시간', '이벤트 태그'를 함께 호출하여 프론트엔드로 전달

#### [NEW] `api/ai-insight.ts`
- DB의 최근 7일 치 데이터를 요약하여 Groq API(`llama-3.3-70b-versatile`)로 추론 분석 도출

#### [NEW] `api/log-metrics.ts`
- 사용자가 입력한 혈당, 수면시간, 스트레스 지수를 Supabase DB에 저장(INSERT 또는 UPDATE)하는 API 작성

---

### 3. Frontend Components (Dashboard UI 개편)

#### [MODIFY] `src/pages/Dashboard.tsx` & `src/pages/SampleDashboard.tsx`
- **신규 라이프로그(이벤트) 표출 컴포넌트 추가**: 
  - 위치: "라이프로그 요약" 위젯 및 "복합 트렌드 그래프"의 **아래**, "Transparent AI Inference" 및 "펌프 오류 분석"의 **위**.
  - 기능: 해당 일자에 운동(예: 1.5시간)이나 이벤트(예: 회식) 데이터가 있을 경우, 시각적으로 눈에 띄는 뱃지(Badge)나 타임라인 형태로 예쁘게 표출.
- **체험(샘플) 대시보드 동기화**: `SampleDashboard.tsx`의 기존 가상 데이터에도 운동 및 회식 데이터를 추가하고 동일하게 표출.
- **수동 기록 모달 UI 전면 개편**: 
  - [혈당] [수면시간] [스트레스] 3가지 탭 구현 및 모바일 반응형 적용
- **모바일 반응형 적용**: 
  - 차트, 카드 위젯, 버튼 크기가 스마트폰 세로 화면에 맞춰 자동으로 재배열

## Verification Plan

### Manual Verification
1. 제공해 드리는 SQL 쿼리를 Supabase 대시보드(SQL Editor)에 붙여넣어 100일 치 가상 데이터를 세팅합니다.
2. Vercel 배포 후 스마트폰 접속 시 화면이 모바일 반응형으로 예쁘게 재배열되었는지 확인합니다.
3. 폰에서 **[수동 기록]** 버튼을 눌러 **혈당 / 수면시간 / 스트레스** 탭 입력 시 DB에 저장되는지 확인합니다.
4. 트렌드 그래프 바로 아래에 **운동 시간 및 회식 등 이벤트 태그**가 예쁜 UI로 표출되어 있는지 확인합니다.
5. `체험하기(SampleDashboard)` 접속 시에도 동일한 UI와 이벤트 태그들이 뜨는지 확인합니다.
