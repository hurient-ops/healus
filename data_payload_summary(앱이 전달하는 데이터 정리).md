# Healus 앱 ➔ 서버 전송 데이터 구조 명세서

현재 Healus 모바일 앱(Flutter) 내 `api_client.dart` 코드를 기준으로, 기기에서 서버(FastAPI)로 쏘아주는 데이터들의 JSON 구조를 구체적으로 정리했습니다.

모든 데이터는 사용자 계정이 아닌 **펌프 기기의 `device_mac` 주소**를 기준으로 식별되며, 서버에 도달하면 이 MAC 주소를 통해 해당하는 유저 계정(User)과 매핑됩니다.

---

## 1. 펌프 일별 주입 이력 전송 (`POST /api/logs`)
사용자가 펌프에서 지난 기록을 동기화할 때 대량(Bulk)으로 전송되는 일별 인슐린 주입량 데이터입니다.

- **엔드포인트:** `POST http://서버주소/api/logs`
- **전송 방식:** Array(배열)로 묶어서 벌크(Bulk) 전송

**[JSON Payload 예시]**
```json
{
  "logs": [
    {
      "device_mac": "00:1A:2B:3C:4D:5E", // 기기 고유 식별자 (필수)
      "month": 7,                        // 월
      "day": 24,                         // 일
      "base_total": 10.5,                // 기초 주입량 총합 (U)
      "eat_total": 5.0,                  // 식사 주입량 총합 (U)
      "morning_total": 3.0,              // 아침 주입량
      "afternoon_total": 4.0,            // 점심 주입량
      "evening_total": 3.5,              // 저녁 주입량
      "append_total": 0.0,               // 추가 주입량
      "created_at": "2026-07-24T12:00:00.000" // 앱에서 기록된 시간
    },
    {
      // ... 추가 로그들
    }
  ]
}
```

---

## 2. 펌프 블루투스 원시 패킷 전송 (`POST /api/raw_logs`)
스마트폰과 펌프가 블루투스(BLE)로 통신할 때 오가는 모든 바이트(Byte) 패킷을 모아서 서버로 보냅니다. 통신 단절 시 앱 내 SQLite(오프라인 큐)에 저장해두었다가 인터넷이 연결되면 한 번에 50개씩 전송합니다.

- **엔드포인트:** `POST http://서버주소/api/raw_logs`
- **전송 방식:** Array(배열)로 묶어서 벌크(Bulk) 전송

**[JSON Payload 예시]**
```json
{
  "packets": [
    {
      "id": 152,                         // 모바일 앱 자체 SQLite 큐 ID (서버에서는 무시됨)
      "device_mac": "00:1A:2B:3C:4D:5E", // 기기 고유 식별자 (필수)
      "direction": "RX",                 // TX (스마트폰 -> 펌프) 또는 RX (펌프 -> 스마트폰)
      "payload_hex": "01 04 a2 ff 30",   // 원시 바이트 데이터를 Hex 스트링으로 변환한 값
      "timestamp": "2026-07-24T12:05:33.123" // 패킷 송수신 발생 시간
    }
  ]
}
```

---

## 💡 참고 사항 (향후 확장)
현재 모바일 앱의 `api_client.dart`에는 **혈당(Blood Glucose)** 데이터를 전송하는 로직이 아직 포함되어 있지 않습니다. (현재 백엔드에는 스키마가 존재하며 더미 데이터로만 채워지고 있습니다.)

만약 향후 앱에서 연속혈당측정기(CGM) 데이터를 수집하여 전송한다면, 위와 동일하게 `device_mac`을 포함한 배열 형태의 `/api/bg_logs`(가칭) 전송 로직이 추가될 예정입니다.
