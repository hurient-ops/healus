# 상태 모달 원복 및 대시보드 에러 상태 이상 버그 수정 (2차 정밀 수정)

## 1. "idle 상태가 아닐 때 토스트 메시지가 안 뜨는 문제" 수정 완료
- **원인 분석**: idle 상태가 아닐 경우 주입이 거절(`onInjectionRejected`)되고 "주입 요청을 취소합니다"라는 토스트가 뜹니다. 하지만 이때 사용자가 버튼을 눌러 에러가 발생하거나 기기 자체적으로 강제로 대시보드 화면 전환(`_loadDashboardPage`)을 일으킬 경우, 페이지가 즉시 새로고침되면서 화면에 토스트가 채 그려지기도 전에 증발해 버리는 타이밍 이슈(Race condition)가 발생하고 있었습니다.
- **해결 방법**: `meal_injection`, `add_injection`, `dining`, `exercise` 4개 화면의 `onInjectionRejected` 함수 내에 `localStorage.setItem('showToastMsg', '주입 요청을 취소합니다.');`를 추가했습니다. 이를 통해 만약 화면이 대시보드로 즉시 튕겨나가더라도, 대시보드 진입 시 해당 저장값을 읽어 토스트 메시지를 다시 띄워주게 되어 더 이상 토스트 메시지가 누락되지 않습니다.

## 2. "에러 패킷 수신 시 멀티모달과 배지가 뜨는 문제" 심층 원인 분석 및 완전 차단
- **원인 분석**: 사용자가 "주입하기"를 눌렀을 때, 기기는 일단 정상적으로 idle(0x01) 응답을 줍니다. 그래서 앱은 "아, 주입을 시작해도 되겠다!"라고 판단하여 대시보드로 이동하며 `?modal=request` 파라미터를 붙여 모달을 띄웁니다. 
그러나 바로 그 직후, 기기가 하드웨어적인 문제(예: 모터 오류, 배터리 등)를 인지하고 **에러 패킷(0x19)** 을 뿜어냅니다. 
기존의 `ErrorInterceptor`는 현재 화면이 이미 "대시보드"일 경우 페이지 전환 로직을 건너뛰도록 설계되어 있었습니다(`if (!_currentUrl.contains("dashboard"))`). 결국 모달이 열린 채로 페이지가 유지되어 에러가 무시되는 듯한 버그가 발생한 것입니다.
- **해결 방법**: `main.dart`의 `ErrorInterceptor` 로직에서 `!_currentUrl.contains("dashboard/code.html")` 검사 조건을 과감히 제거했습니다. 
이제 주입 성공 후 대시보드에 막 진입해 모달이 띄워진 상태라 하더라도, 에러 패킷이 날아오면 **무조건 대시보드를 파라미터 없이 새로고침**하여 강제로 덮어씌웁니다. 
새로고침을 하면서 URL의 파라미터(`modal=request`)가 제거되므로 모달은 영구적으로 닫히고, 앞선 조치(`isInjecting = false`)에 의해 배지 역시 깔끔하게 사라집니다.
