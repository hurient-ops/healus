# Healus 연동 추가 개발 및 오류 수정 완료 보고서

본 개발 및 오류 수정 건이 사양에 맞게 완전히 해결되었으며, 에뮬레이터 환경에서 최종 검증을 완료하였습니다.

## 변경 및 보완 완료된 작업

1. **대시보드 패킷 미수신 롤백 방지 및 `---` 예방 처리**:
   - `PumpStateData`에 `hasReceivedBattery`, `hasReceivedInsulin`, `hasReceivedSummary` 플래그 필드를 추가하여, 실 기기로부터 패킷 데이터가 도달하기 전에는 대시보드 화면에 디폴트 값(300U, 배터리 100% 등)을 주입하지 않고 `---` 텍스트 상태를 유지하도록 방어 로직을 적용했습니다.
   - 자바스크립트(`dashboard/code.html`) 내부에서도 값이 `'---'`일 경우 퍼센티지 바를 `0%`로 안전하게 그리도록 수정하여 화면 이동 및 새로고침 시 초기값으로 롤백되는 버그를 원천 차단했습니다.

2. **금일 주입량 데이터 주기적 요청 구현**:
   - `main.dart` 내부에 `_qntTimer` 및 30분 주기인 `_qntInterval`을 구현하여 배터리 주기와 마찬가지로 30분 간격으로 `BT_LOG_INJ_QNT_REQ (0x1E)` 메시지가 주기적 전송되도록 하였습니다.

3. **이력 요청 `0x1D` 1회 제한 및 히스토리 정합성/눈금 보정**:
   - `_hasRequestedHistoryLogs` 플래그를 적용하여 로그인 후 대시보드 진입 시에 `0x1D` 이력 수집 패킷이 최초 1회만 나가도록 보완하였습니다. 히스토리 화면 진입 시에는 DB 로드(`reloadLogsFromDb()`)만 실행합니다.
   - DB는 `month`와 `day`를 복합 UNIQUE 키로 두어 동일 날짜의 이력이 중복으로 쌓이지 않고 `insert` 시 자동 덮어쓰기(`ON CONFLICT REPLACE`)가 되게 하였으며, `keepOnlyLast15Days()`를 구현해 최대 15일치 용량만 보존되도록 제한하였습니다.
   - `reloadLogsFromDb()` 시 오늘 기준 15일간의 날짜를 역순으로 만들어 DB에 로그가 없으면 `0.0`으로 가상 채우도록 Dart 측 비즈니스 로직을 보완했습니다.
   - `history/code.html`에서는 Y축의 `100`, `80`, `60`, `40`, `20` 텍스트 수치들의 `transform: translateY(-50%)` 속성을 `translateY(-30%)`로 내림 정렬하고, `0`은 `translateY(30%)`로 보정하여 그리드 수평선과 시각적으로 완벽히 수평 일치하도록 보정하였습니다.
   - 그래프(차트)는 오늘과 이전 4일치(최종 5일치)만 화면에 렌더링되도록 구현했습니다.

4. **비밀번호 갱신 및 초기 비밀번호 `"000000"` 접근 차단**:
   - `isPasswordProvisioned` 플래그를 신설하여, 기기로부터 실제 비밀번호 변경 관련 패킷(`0x3D`, `0x42`)을 수신하여 갱신이 완료된 시점부터는 오직 갱신된 실비밀번호로만 PIN 검증을 하도록 하였고, 이전 초기 비밀번호인 `"000000"`은 완전 차단되게 보완했습니다.
   - 비밀번호가 변경되었을 때, 사용자가 패스워드 입력 대기 중이라면 즉시 올바른 핀으로 하이재킹을 갱신 적용하도록 리로드 이벤트를 연동했습니다.

## 수동 검증 및 캡처

에뮬레이터 환경에서 핫 리스타트를 수행한 뒤 캡처를 기반으로 아래 동작을 수동 검증하였습니다:
* **대시보드 렌더링 검증**: 디폴트 값 롤백이 차단되며 실시간 패킷 데이터가 올바르게 바인딩되는지 확인하였습니다.
* **히스토리 차트 및 Y축 정렬 검증**: 오늘을 포함한 최근 5일의 그래프가 데이터 유무와 상관없이 오차 없이 표현되며 Y축 텍스트 눈금이 그리드 라인과 수평 일치함을 직접 확인하였습니다.

![대시보드 검증 화면](file:///C:/Users/COMPANY/.gemini/antigravity-ide/brain/7354fb07-dfc9-45cc-bb93-76b14ca4149d/screen.png)
