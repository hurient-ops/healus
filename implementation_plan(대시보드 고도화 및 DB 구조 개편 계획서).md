# 대시보드 고도화 및 DB 구조 개편 계획서

요청해주신 8가지 요구사항(펌프 패킷 통신 구조 반영, DB 변경, 정렬 기준 변경, 페이지네이션 추가, 404 에러 해결 등)을 모두 반영하기 위한 상세 구현 계획입니다.

## 제안하는 변경 사항

### 1. 데이터베이스(Supabase) 구조 및 초기화 스크립트 전면 개편
- `E:\projects\healus-web\supabase_schema_and_data.sql`
  - **테이블 재생성 및 초기화**: `DELETE` 문 대신 테이블 `DROP` 및 재생성 로직을 강화하여 과거 데이터를 말끔히 초기화합니다.
  - **신규 컬럼 추가**: `reception_hours` (회식시간), `notes` (특이사항/메모) 컬럼을 `pump_logs` 테이블에 추가합니다.
  - **모의 데이터 기준일 변경**: 오늘(Today) 기준 '어제(Yesterday)'까지 100일치의 데이터를 생성하도록 시뮬레이션 기간을 조정합니다.
  - **오류 상태 데이터 주입**: 기존에 비어있던 `error_count`, `error_types` (주입불가, 일시정지, 1일 초과량 주입, 펌프에러 등) 데이터가 100일 중 간헐적으로 발생하도록 로직을 추가합니다.

### 2. Vercel 새로고침(F5) 404 NOT_FOUND 에러 해결
- `E:\projects\healus-web\vercel.json` (신규 파일)
  - React Router(SPA) 환경에서 Vercel이 모든 URL 경로를 `index.html`로 돌려보내도록(Rewrite) 규칙을 설정하여 새로고침 시 발생하는 404 에러를 원천 차단합니다.

### 3. Vercel API 서버 연동 항목 추가
- `api/dashboard.ts` 및 `api/ai-insight.ts`
  - 프론트엔드로 `reception_hours` 및 `notes` 데이터를 전달하도록 매핑 규칙을 업데이트합니다.
  - AI 프롬프트 구성 시 회식 시간과 특이사항 텍스트가 포함되도록 하여 추론 정확도를 높입니다.

### 4. 프론트엔드 (Dashboard.tsx & SampleDashboard.tsx) UI/UX 개선
- **인슐린 & 혈당 복합 트렌드 차트 (30일)**
  - **날짜 정렬**: 왼쪽이 과거, 오른쪽이 최신(어제)이 되도록 차트 데이터 정렬을 오름차순으로 변경합니다.
  - **과거 데이터 이동(Pagination)**: 차트 상단에 `< 이전 30일`, `다음 30일 >` 화살표 버튼을 추가하여, 이전 데이터를 탐색할 수 있는 기능을 구현합니다. (이전 데이터가 없으면 화살표 비활성화)
- **라이프로그 요약 (최근 7일)**
  - 리스트의 맨 위가 가장 최신(어제), 맨 아래가 7일 전이 되도록 내림차순 정렬합니다.
- **최근 주요 라이프로그 이벤트 (가로 스크롤 영역)**
  - 가로 스크롤의 맨 왼쪽이 과거, **맨 오른쪽이 최신(어제)**이 되도록 정렬합니다.
  - **배지 및 아이콘 규칙**: 기존의 단순 태그 문자열 대신, `운동(h)`과 `회식(h)` 값만 배지로 표시합니다. 만약 `notes`(특이사항)가 있는 날은 '메모 아이콘(💬)'을 표시하고, 마우스오버(Hover) 시 툴팁이나 말풍선으로 특이사항 텍스트를 읽을 수 있게 UI를 개선합니다.

### 5. 매일(오늘) 데이터를 삽입하는 신규 SQL 스크립트 작성
- **오늘 날짜 단건 데이터 주입 SQL** 코드를 별도의 파일(`insert_today_data.sql` 또는 아티팩트)로 작성하여 제공해 드립니다.

---

> [!IMPORTANT]
> **사용자 확인 요청**
> 1. 제공해 주신 펌프 패킷 통신 구조(BT_LOG_REQ 등)는 향후 실제 백엔드/모바일 앱 개발 시 펌프 동기화 로직의 아키텍처 기준으로 삼겠습니다. 웹 프론트엔드 입장에서는 DB에 적재된 결과값을 조회하므로, DB 스키마 구조(운동시간, 회식시간, 에러 등)에 해당 내용을 충실히 반영하였습니다.
> 2. `SampleDashboard.tsx`(체험 대시보드)도 위의 실제 대시보드와 완전히 동일한 UI 규칙과 목업(Mock) 데이터를 가지도록 함께 수정됩니다.

위 계획대로 수정 및 파일 추가를 진행해도 될까요? **[Proceed]** 버튼을 눌러주시거나 추가 의견을 편하게 말씀해 주세요!
