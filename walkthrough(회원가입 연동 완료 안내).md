# 회원가입 연동 완료 안내

웹 프론트엔드(`healus-web`)와 백엔드(`healus-backend`) 간의 회원가입 1차 연동 작업을 모두 성공적으로 마쳤습니다!

## 주요 변경 사항

### 1. 프론트엔드 (Signup.tsx)
- **UI 직관성 개선**: MAC 주소 입력 칸의 Placeholder에서 불필요한 "S/N 또는" 텍스트를 제거하여 깔끔하게 **"MAC 주소 입력 (예: 00:1A:2B...)"** 만 보이도록 수정했습니다.
- **실시간 폼(Form) 관리**: 사용자가 입력한 이름, 이메일, 비밀번호, MAC 주소를 상태(State)로 실시간 추적합니다.
- **Axios 연동 및 예외 처리**: 가입 버튼을 누르면 `http://localhost:8000/api/auth/signup` 으로 데이터를 쏘아주며, 이메일이 이미 존재하거나 MAC 주소가 중복되면 화면에 **빨간색 에러 메시지 상자**를 띄워 사용자에게 알려줍니다.
- **가입 성공 시**: 알림창("회원가입이 완료되었습니다")을 띄운 뒤 자동으로 로그인 화면(`/login`)으로 넘어갑니다.

### 2. 백엔드 (auth.py & main.py)
- **비밀번호 암호화**: `passlib[bcrypt]` 라이브러리를 적용하여, 사용자의 비밀번호가 DB에 날것(평문)으로 저장되지 않고 안전한 해시(Hash) 형태로 암호화되어 저장됩니다.
- **중복 검사(Validation)**: 
  - 누군가 이미 사용 중인 이메일이라면 가입을 차단합니다. (400 에러 반환)
  - 만약 다른 사람이 쓰고 있는 펌프의 MAC 주소를 실수로 적었다면 이 역시 사전에 차단하여 데이터가 꼬이는 것을 방지합니다.
- **UUID 자동 발급**: 앞서 수정한 아키텍처에 맞춰, 신규 회원의 PK는 128비트 길이의 안전한 UUID로 생성됩니다.

## 🚀 다음 단계 제안
가입 로직이 완성되었으니, 자연스러운 흐름을 위해 다음 작업들 중 하나를 진행하는 것이 좋습니다.

1. **로그인 기능 연동**: 현재 껍데기만 있는 `/login` 화면에 진짜 JWT 로그인 API 기능을 연결하기
2. **테스트 구동**: 양쪽 터미널에서 백엔드와 프론트엔드를 동시에 켜고 화면에서 직접 가입을 테스트해 보기

어느 것을 먼저 진행해 드릴까요?
