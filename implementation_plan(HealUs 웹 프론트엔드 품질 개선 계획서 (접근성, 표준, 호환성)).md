# HealUs 웹 프론트엔드 품질 개선 계획서 (접근성, 표준, 호환성)

본 계획서는 HealUs 웹 대시보드(`healus-web`)가 모든 환경의 사용자에게 동일하고 편리한 경험을 제공할 수 있도록 **웹 접근성(WCAG)**, **웹 표준(W3C)**, **웹 호환성(Cross-Browsing)** 요건을 달성하기 위한 구체적인 작업 계획입니다.

## User Review Required

> [!IMPORTANT]  
> 본 작업은 화면의 외형적인 디자인을 크게 바꾸지는 않지만, 내부 HTML 구조와 CSS 설정 파일, 그리고 `ESLint` 등 설정 파일들이 다수 변경됩니다. 적용 방향성이 맞다면 승인(Proceed)해 주시면 즉시 코드 개선 작업을 시작하겠습니다.

---

## 1. 웹 접근성 (Web Accessibility) 적용 계획

**목표:** 스크린 리더 사용자 및 키보드 전용 사용자가 모든 핵심 데이터를 인지하고 조작할 수 있도록 개선.

### 1-1. 스크린 리더(TTS) 완벽 지원
- **차트 데이터 대체 텍스트 제공**: ApexCharts/Recharts로 그려지는 시각적 그래프에 `aria-label`을 부여하고, 화면에는 보이지 않지만 스크린 리더만 읽을 수 있는 요약 텍스트(`<span className="sr-only">`)를 삽입합니다.
- **아이콘 의미 부여**: 배터리, 주사기 등 모든 `lucide-react` 아이콘 요소에 `aria-hidden="true"`를 적용하고 스크린리더용 대체 텍스트를 부여합니다.

### 1-2. 키보드 네비게이션 개선
- **논리적 탭(Tab) 이동 순서 설정**: 네비게이션 메뉴 ➡️ 대시보드 타이틀 ➡️ 상단 버튼(화면 캡쳐 등) ➡️ 각 차트 영역 순서로 `tabindex`를 정비합니다.
- **가시적인 포커스(Focus) 링 제공**: 키보드로 조작 시 현재 활성화된 버튼이나 링크를 명확히 알 수 있도록 CSS `:focus-visible` 속성을 사용하여 뚜렷한 포커스 테두리를 추가합니다.

### 1-3. 모션 제어 (전정기관 장애 배려)
- **애니메이션 줄이기 설정 존중**: 프로그레스 바가 차오르는 애니메이션에 `@media (prefers-reduced-motion: reduce)` CSS 룰을 추가하여 기기 설정에서 '애니메이션 줄이기'를 활성화한 사용자의 경우 애니메이션을 정지된 상태로 렌더링합니다.

---

## 2. 웹 표준 (Web Standards) 적용 계획

**목표:** W3C 권고안을 준수하여 구조(HTML)와 표현(CSS)이 완벽히 분리된 클린 코드로 리팩토링.

### 2-1. 시맨틱(Semantic) 마크업 전환
- 단순 `<div>`로 묶인 레이아웃 영역들을 목적에 맞는 시맨틱 태그(`<header>`, `<main>`, `<section>`, `<article>`, `<nav>`)로 치환하여 보조기기와 검색 엔진(SEO)이 문서 구조를 쉽게 파악하게 합니다.

### 2-2. 린트(Linting) 도구 엄격화
- `healus-web` 프로젝트 내에 **접근성 전용 ESLint 플러그인**(`eslint-plugin-jsx-a11y`)을 설치 및 구성하여, 향후 개발 시에도 `alt` 속성 누락이나 잘못된 `aria` 속성 사용을 빌드 타임에 차단하도록 표준 규칙을 강제합니다.

---

## 3. 웹 호환성 (Web Compatibility) 적용 계획

**목표:** 최신 브라우저뿐만 아니라 구형 브라우저 및 다양한 크기의 모바일 기기에서도 화면이 깨지지 않고 정상 동작하도록 보장.

### 3-1. CSS 벤더 프리픽스 자동화
- Vite/Tailwind 환경의 빌드 파이프라인에 `Autoprefixer`가 제대로 동작하는지 타겟 브라우저 리스트(`browserslist`)를 명시적으로 설정하여, Safari 구형 버전 등에서도 최신 CSS 레이아웃(Flex/Grid)이 깨지지 않게 보장합니다.

### 3-2. 유동적 텍스트 크기(Zoom) 대응
- 사용자가 브라우저 글꼴 크기를 200%까지 강제 확대하더라도 글씨가 컨테이너 밖으로 삐져나오거나 잘리지 않게, 고정 크기(`px`) 대신 유동 크기(`rem`, `%`) 단위 사용 및 `break-keep`, `break-words` 등 CSS 텍스트 줄바꿈 방어 로직을 추가합니다.

---

## 4. 검증 및 테스트 계획 (Verification Plan)

작업이 완료된 후 다음의 방법으로 품질을 검증합니다.

### 자동화/기계적 테스트
- **Lighthouse 웹 접근성 평가**: Chrome Lighthouse 측정 탭을 이용해 접근성(Accessibility) 점수를 95점 이상 달성했는지 검증.
- **W3C Validator**: 빌드된 정적 HTML이 W3C 마크업 표준에 위배되지 않는지 검사.

### 수동 테스트 (Manual Verification)
- 마우스 없이 **오직 Tab 키와 Enter 키만으로** 전체 화면 캡쳐, 레포트 출력, 기록 추가 모달 창 열기 등의 동작이 가능한지 직접 검증.
- 맥북/윈도우의 기본 스크린 리더(VoiceOver, NVDA)를 켜고 차트 위를 지날 때 데이터가 소리로 읽히는지 직접 청취 검증.
