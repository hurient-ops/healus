# HealUs 대시보드 데이터 보존 및 히스토리 Y축 수평 정렬 수정 완료 보고서

본 개발 및 오류 수정 건이 사용자의 세부 요건에 맞게 완전히 해결되었으며, 에뮬레이터 환경에서 최종 검증을 완료하였습니다.

---

## 🛠️ 변경 및 개선 사항

1. **실측 패킷 수신 시 모의 데이터 롤백 원천 차단**
   - `PumpStateData` 및 `BasalSyncState`에 실측 패킷 수신 여부를 기록하는 플래그(`hasReceivedRealSummary`, `hasReceivedRealBattery`, `hasReceivedRealInsulin`, `hasReceivedRealMeal`, `hasReceivedRealLogs`)를 추가하였습니다.
   - `isRealPacket` 플래그를 도입하여 BLE 기기로부터 수신된 실제 패킷(혹은 수동 디버그 패킷)이 1회라도 인입되면, 이후 주기적인 펌프 상태 조회에서 가상 기기(`MockBleService`)가 보내는 초기화 하드코딩 데이터를 무시하고 실측 데이터를 앱 종료 시까지 계속 보존하도록 상태 관리를 강화했습니다.

2. **식사 설정 자동 저장 기능**
   - 식사설정 요청 메시지 `BT_EAT_VALUE_REQ(0x2D)`에 대해 수신한 `BT_EAT_VALUE_RES(0x2E)` 패킷 데이터는 사용자가 대화상자에서 명시적으로 '저장' 버튼을 누르지 않아도 자동으로 디바이스 및 로컬 상태에 즉시 저장되도록 개선하여 편의성을 높였습니다.

3. **최근 송신 패킷 로그 유실 문제 해결**
   - 암호 인증 성공 후 대시보드 최초 진입 단계에서 이력 요청 `BT_LOG_REQ(0x1D)` 패킷이 누락되지 않고 확실하게 송신되도록 프로세스 트리거를 동기화하였습니다.

4. **히스토리 Y축 눈금 수평선 불일치 해결**
   - Y축 눈금선 글자 높이가 수평 그리드선보다 글자크기 2~3개 정도 위에 붕 떠서 어긋나 있던 문제를 분석한 결과, 차트 본체 위에 존재하는 날짜 로우(`chart-date-row`) 높이로 인한 오프셋 누락이 원인이었습니다.
   - `ui_design/history/code.html` 파일 내부의 Y축 레이블 컬럼 최상단에 동일한 높이의 spacer 영역을 삽입하여 Y축 눈금선(100, 80, 60, 40, 20, 0)과 수평 그리드가 픽셀 단위로 정확하게 일치하도록 정렬 구조를 수정했습니다.

---

## 📷 실시간 동작 검증 및 화면 캡처

에뮬레이터 환경에서 가상 BLE 패킷 수동 제어 패널을 통해 실측 인슐린 잔량 응답 패킷(`0x16`, `150.0 U`)을 강제 주입하여 검증한 결과는 다음과 같습니다:

1. **실시간 데이터 수치 갱신 검증**
   - 수동 패킷 주입을 수행한 즉시 대시보드의 인슐린 잔량이 `150.0 / 300 U` 로 정확히 갱신되는 것을 확인했습니다.
   
2. **새로고침 및 화면 이동 복귀 후 데이터 보존 검증**
   - 새로고침 버튼(동기화 아이콘)을 눌러 리로드를 수행하거나 하단 탭을 통해 히스토리 화면으로 이동한 후 다시 홈 화면으로 복귀하여도 데이터가 롤백되지 않고 `150.0 U`가 계속 유지되는 것을 확인했습니다.
   
3. **히스토리 Y축 수평 정렬 정밀 검증**
   - 히스토리 화면의 Y축 텍스트 레이블(100 ~ 0)의 중심선이 차트 영역의 수평 그리드선과 한 치의 오차도 없이 완벽하게 정렬되었습니다.

### 🖼️ 최종 검증 화면 캡처
![대시보드 실측 보존 완료](file:///C:/Users/COMPANY/.gemini/antigravity-ide/brain/7354fb07-dfc9-45cc-bb93-76b14ca4149d/screen.png)
