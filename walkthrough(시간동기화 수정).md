# 시간 동기화(Time Synchronization) 로직 개편 완료

요청하신 요구사항에 맞추어 시간 동기화 과정을 앱의 첫 진입점인 대시보드로 이관하고 성공/실패 흐름을 강화하였습니다.

## 변경 사항 요약

1. **기존 로직 제거**
   - `ble_handshake_controller.dart` 내부에 있던 0x06 송신 및 0x44 대기, 실패 시 시스템 리셋 처리 부분(약 50라인)을 깔끔하게 제거했습니다.

2. **신규 시간 동기화 컨트롤러 연동**
   - **위치:** `lib/services/ble/time_sync_controller.dart`
   - 최초 대시보드 진입 시에 1회 호출되어 시간 동기화를 시도합니다.
   - **500ms 타임아웃 룰 적용:** 패킷 수신 대기 시간이 500ms를 초과하면 타임아웃(실패)으로 처리됩니다.
   - **무한 대기 바이패스:** 수동 테스트 편의를 위해 가상 패널의 `timeoutHoldProvider` 값이 활성화되어 있으면 500ms 룰을 건너뛰고 응답(0x44)이 올 때까지 계속 대기합니다.
   - **30분 주기 재시도:** 한 번이라도 실패하면 백그라운드 타이머를 가동시켜 매 30분마다 재시도합니다. 도중에 언제라도 한 번 성공하면 이 루프는 영구 종료됩니다.

3. **UI 통일 (커스텀 토스트)**
   - 대시보드(`dashboard/code.html`) 내부 로컬 함수로 쓰이던 `showToast`를 외부(Flutter)에서도 원격 호출이 가능하도록 `window.showToast`로 전역 공개했습니다.
   - 에러 시 **"시간 동기화에 실패했습니다"**라는 텍스트가 대시보드 하단의 '검은 바탕에 흰 글씨(둥근 형태)' 디자인 그대로 매끄럽게 노출됩니다.

## 검증 플랜 제안

변경된 코드가 앱(에뮬레이터)에 정상적으로 핫 리스타트(Hot Restart)되어 반영되었습니다.

- **성공 검증:** 초기 접속 시 가상 장치 패널에서 0x44 정상 응답이 오는지 확인.
- **실패 및 토스트 UI 검증:** 
  - 앱을 완전히 종료(킬)한 뒤 재실행하세요.
  - 가상 장치 제어 패널에서 일부러 타임아웃을 발생시키거나, 틀린 시간 배열을 쏘도록 둔 상태로 대시보드 화면에 진입해 보세요.
  - 진입 직후 500ms 뒤 화면 하단에 **"시간 동기화에 실패했습니다"** 토스트 팝업이 뜨는지 확인하시면 됩니다.
