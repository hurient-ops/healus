# 대시보드 화면 캡쳐 및 레포트(PDF) 생성 기능 추가 계획

환자가 자신의 기록을 주치의에게 공유하거나 개인적으로 보관할 수 있도록, 대시보드 화면을 이미지로 저장하고 PDF 형태의 깔끔한 레포트로 출력하는 기능을 추가합니다.

## User Review Required
> [!NOTE]
> PDF 레포트를 생성하기 위해 외부 라이브러리(`html2canvas`, `jspdf`)가 설치됩니다.
> 레포트는 A4 용지 비율에 맞춰 대시보드의 내용(차트, 요약 지표, AI 분석 결과 등)을 그대로 담아내게 됩니다.

## Proposed Changes

### 프론트엔드 (healus-web)
#### [NEW] 라이브러리 설치
- `html2canvas`: 브라우저 화면(DOM)을 고화질 캔버스 이미지로 변환합니다.
- `jspdf`: 변환된 이미지를 A4 사이즈의 PDF 문서로 조립하여 다운로드하게 해줍니다.

#### [MODIFY] `src/pages/Dashboard.tsx`
- **UI 및 아이콘 추가**: 상단 "수동 기록" 버튼 옆에 기능 버튼 2개를 추가합니다. 시각적 직관성을 높이기 위해 `lucide-react` 라이브러리의 아이콘을 활용합니다.
  - 📸 `Camera` 아이콘 + "화면 캡쳐" 버튼: 현재 대시보드를 PNG로 즉시 저장
  - 📄 `FileDown` 아이콘 + "레포트 PDF" 버튼: A4 규격의 리포트로 생성하여 저장
- **캡쳐 로직(`handleCapture`)**: 대시보드 영역을 지정하여 `html2canvas`로 찍은 뒤, `.png` 이미지 파일로 즉시 다운로드시킵니다.
- **레포트 로직(`handleReport`)**: 대시보드 전체를 이미지로 뜬 후, `jsPDF`를 이용해 A4 용지 규격에 딱 맞게 리사이징하여 여백을 주고 `.pdf` 문서로 출력(다운로드)합니다.

## Verification Plan
1. 패키지 설치 후 웹 서버가 정상 구동되는지 확인합니다.
2. [화면 캡쳐] 버튼 클릭 시 현재 내 대시보드가 잘리지 않고 고화질 PNG 이미지로 저장되는지 테스트합니다.
3. [레포트 PDF] 버튼 클릭 시 여백이 적용된 A4 규격의 PDF 파일이 다운로드되며, 내용이 깨짐 없이 잘 담겨있는지 확인합니다.
